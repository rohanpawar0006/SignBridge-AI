{
  "name": "signbridge-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Powered Real-Time Bidirectional Indian Sign Language (ISL) Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "lint": "cd frontend && npm run lint"
  }
}
